const CustomTitle = ({ children }: { children: React.ReactNode }) => (
  <p className="text-sm text-success uppercase tracking-widest font-semibold mb-1">{children}</p>
);

export default CustomTitle;
